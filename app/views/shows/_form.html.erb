<%= form_for @show do |f| %>
  <h3>Create your show</h3>

  <% @show.errors.full_messages.each do |msg| %>
    <div class="alert-message error">
      <p>
        <%= msg %>
      </p>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <div class="input">
      <%= f.text_field :title %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit :class => "primary btn large" %>
  </div>
<% end %>
